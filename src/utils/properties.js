/**
 * 常量配置文件
 */
const PUBLICINFO = {
  PUBLICTITLE: "待定",
};

const API = {
  VERSION: "1.0.0",
};

// 文件类型校验
const FILETYPES = ["image/jpg", "image/png", "image/jpeg", "image/bmp"];

// 缓存key
const SESSIONKEY = {
  // token
  ACCESS_TOKEN: "token",
  // refresh_token
  REFRESH_TOKEN: "refreshToken",
};

export { PUBLICINFO, API, FILETYPES, SESSIONKEY };
